add_namespace = gal_war

country_event = {
	id = gal_war.1
	title = gal_war.1.t
	desc = gal_war.1.desc
	fire_only_once = yes
	picture = ""
	is_triggered_only = yes
	option = {
		name = gal_war.1.a
		ai_chance = {
			base = 100
		}
		SWA = {
			declare_war_on = {
				target = GAL
				type = annex_everything
			}
		}
	}
}