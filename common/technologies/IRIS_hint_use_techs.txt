technologies = {
	#显示地区控制力效果
	IRIS_display_region_control_effect = {

		allow = {
			always = no
		}

		research_cost = 1
		start_year = 3090

		on_research_complete = {
			meta_effect = {
				text = {
					ROOT = {
						swap_ideas = {
							remove_idea = [NowLevel]
							add_idea = [NextLevel]
						}
					}
				}
				NextLevel = "[?NextLevel.GetTokenKey]"
				NowLevel = "[?NowLevel.GetTokenKey]"
			}
		}
		show_effect_as_desc = yes
	}
}