# 高举新利伯特主义
faction_manifest_raise_high_new_liebertism = {
	name = raise_high_new_liebertism_goal_name
	description = raise_high_new_liebertism_goal_desc
	is_manifest = yes
	ratio_progress = {
		total_amount_collection = collection:states_in_the_across_pacific_ocean
		completed_amount_collection = collection:faction_states_in_the_across_pacific_ocean

		range = { 
			max = 0.67
		}
		
		scale = {
			party_popularity_stability_factor = 0.10
		}
		
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
				modifier = { 
					monthly_population = 0.15
					generate_wargoal_tension = -0.15
				}
			}
		}
	}
}