@manifest_fulfilled_value = 0.75

###早樱共和同萌
faction_manifest_unify_SAO = { #统一早樱
	name = unify_SAO_goal_name
	description = unify_SAO_goal_desc
	is_manifest = yes

    ratio_progress = {
		total_amount_collection = collection:states_in_SAO
		completed_amount_collection = collection:faction_states_in_SAO
		
		scale = {
			industrial_capacity_factory = 0.1
			industrial_capacity_dockyard = 0.1
			army_org_factor = 0.1
			political_power_factor = 0.15
		}
		
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
	
				modifier = {
					war_support_factor = 0.15
				}			
			}
		}
	}

}

###沃雅基阵线
faction_manifest_expand_front_voyage = { #春阳下的沃雅基
    name = expand_front_voyage_goal_name
	description = expand_front_voyage_goal_desc
	is_manifest = yes

    ratio_progress = {
		total_amount_collection = collection:states_in_the_voyage
		completed_amount_collection = collection:faction_states_in_the_voyage
		
		scale = {
			army_attack_against_major_factor = 0.15
			army_breakthrough_against_major_factor = 0.15
			army_defence_against_major_factor = 0.15
		}
		
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
	
				modifier = {
					stability_factor = 0.10
				}			
			}
		}
	}
}

###帝国同萌
faction_manifest_rule_north_asyia = { #阿西娅霸主
    name = rule_north_asyia_goal_name
	description = rule_north_asyia_goal_desc
	is_manifest = yes

    ratio_progress = {
		total_amount_collection = collection:states_in_north_asyia
		completed_amount_collection = collection:faction_states_in_north_asyia
		
		scale = {
			conscription_factor = 0.1
			army_attack_against_minor_factor = 0.1
		}
		
		progress_sections = {
			manifest_fulfilled = {
				min = @manifest_fulfilled_value
	
				modifier = {
					research_speed_factor = 0.05
				}			
			}
		}
	}
}