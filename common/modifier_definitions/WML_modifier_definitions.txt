#每月资金
colony_monthly_funding = {
	color_type = good
	value_type = percentage
	precision = 2
}
#每月资金 修正
colony_monthly_funding_factor = {
	color_type = good
	value_type = percentage
	precision = 2
}
#每月黑帮影响力（地区）
colony_gangster_influence_region = {
	color_type = bad
	value_type = percentage
	precision = 2
}
#每月黑帮影响力（全国）
colony_gangster_influence_nationwide = {
	color_type = bad
	value_type = percentage
	precision = 2
}
#每月腐败度（地区）
colony_corruption_region = {
	color_type = bad
	value_type = percentage
	precision = 2
}
#每月腐败度（全国）
colony_corruption_nationwide = {
	color_type = bad
	value_type = percentage
	precision = 2
}