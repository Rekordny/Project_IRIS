on_actions = {
    on_daily_WRQ = {
        effect = {
            for_each_scope_loop = {
                array = global.IRIS_has_region_control_mechanism
                IRIS_daily_region_occupation_progress_refresh = yes
            }
        }
    }
}