NSN_temp_category = {
    NSN_attack_SSN = {
        available = {
            is_subject = no
            NOT = {
                is_ally_with = SSN
            }
            OR = {
                RVA = {
                    is_ally_with = ROOT
                    OR = {
                        has_war_with = SSN
                        has_war_with = USC
                    }
                }
                AND = {
                    has_global_flag = USF_CIV_WAR_STARTED
                    NOT = {
                        has_global_flag = USF_CIV_WAR_ENDED
                    }
                }
            }
            
        }
        visible = {
            country_exists = SSN
        }
        fire_only_once = yes
        days_remove = 30
        remove_effect = {
            remove_ideas = NSN_protect_ourselves
            declare_war_on = {
                target = SSN
                type = annex_everything
            }
        }
        ai_will_do = {
            factor = 1000
        }
    }
}