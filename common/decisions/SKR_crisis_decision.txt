SKR_crisis_decision = {
    SKR_refresh_power_projection_monthly_mission = {
        icon = mission_icon
        is_good = no
		days_mission_timeout = 30
        available = {
            always = no
        }
		activation = {
			always = no
		}
		cancel_trigger = {
			has_global_flag = SKR_CRISIS_ENDED_FLAG
		}
		complete_effect = {

		}
        timeout_effect = {
            SKR_refresh_power_projection_monthly = yes
            activate_mission = SKR_refresh_power_projection_monthly_mission
        }
    }

    SKR_send_fleet = {
        custom_cost_trigger = {
			command_power > 9
		}
        custom_cost_text = decision_cost_CP_10
        days_remove = 60
        days_re_enable = 30
        complete_effect = {
            set_country_flag = SKR_crisis_in_patrol_flag
        }
        modifier = {
            naval_accidents_chance = 0.15
        }
        remove_effect = {
            set_temp_variable = {power_projection_temp = 4}
            SKR_change_power_projection_effect = yes
            clr_country_flag = SKR_crisis_in_patrol_flag
        }
        ai_will_do = {
            factor = 10
        }
    }

    SKR_send_weapons = {
        available = {
            if = {
                limit = {
                    is_ai = no
                }
                has_equipment = {
                    infantry_equipment > 999
                }
            }
		}
        days_remove = 14
        days_re_enable = 50
        complete_effect = {
            set_country_flag = SKR_crisis_in_sending_weapons
        }
        remove_effect = {
            clr_country_flag = SKR_crisis_in_sending_weapons
            set_temp_variable = {power_projection_temp = 3}
            SKR_change_power_projection_effect = yes
            set_temp_variable = {SKR_tention_temp = 3}
            SKR_change_tension_effect = yes
        }
        ai_will_do = {
            factor = 10
        }
    }

    SKR_send_military_advisor = {
        cost = 15
        days_remove = 60
        days_re_enable = 50
        modifier = {
            experience_gain_army = 0.1
            experience_gain_navy = 0.1
        }
        remove_effect = {
            set_temp_variable = {power_projection_temp = 3}
            SKR_change_power_projection_effect = yes
            set_temp_variable = {SKR_tention_temp = 3}
            SKR_change_tension_effect = yes 
        }
        ai_will_do = {
            factor = 10
        }
    }
}