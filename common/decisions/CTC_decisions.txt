CTC_temp_incoming_war_with_DOC ={
	CTC_temp_war_countdown = {
		activation = {
			has_country_flag = CTC_WAR_INCOMIMG
		}
		days_mission_timeout = 30
		war_with_on_timeout = DOC
		war_with_on_timeout = ELN
		timeout_effect = {
			CTC = { create_wargoal = {
				target = DOC
				type = take_core_state
			} }
			add_ai_strategy = {
				type = support
				id = DOC
				value = -10
			}
			add_ai_strategy = {
				type = protect
				id = DOC
				value = -10
			}
			add_ai_strategy = {
				type = declare_war
				id = DOC
				value = 100
			}
			add_ai_strategy = {
				type = conquer
				id = DOC
				value = 100
			}
		}
	}
}
