CTC_temp_incoming_war_with_DOC ={
	CTC_debug_temp_war_flag = {
		visible = {
			TAG = CTC
			is_testing = yes
		}
		complete_effect = {
			set_country_flag = CTC_WAR_INCOMIMG
		}
	}
	CTC_temp_war_countdown = {
		activation = {
			has_country_flag = CTC_WAR_INCOMIMG
		}
		available = {
			always = no
		}
		fire_only_once = yes
		days_mission_timeout = 90
		war_with_on_timeout = URU
		timeout_effect = {
			CTC = { create_wargoal = {
				target = URU
				type = take_core_state
			} }
			add_ai_strategy = {
				type = declare_war
				id = URU
				value = 30
			}
			clr_country_flag = CTC_WAR_INCOMIMG
			set_country_flag = CTC_WAR_INCOMIMG_2
		}
	}
	CTC_temp_war_countdown_2 = {
		activation = {
			has_country_flag = CTC_WAR_INCOMIMG_2
			OR = {
				ELN = { has_capitulated = yes }
				ELN = { surrender_progress > 0.75 }
			}
		}
		available = {
			always = no
		}
		fire_only_once = yes
		days_mission_timeout = 90
		war_with_on_timeout = ELN
		war_with_on_timeout = URU
		timeout_effect = {
			CTC = { create_wargoal = {
				target = DOC
				type = puppet_wargoal_focus
			} }
			add_ai_strategy = {
				type = declare_war
				id = DOC
				value = 100
			}
		}
	}
}
