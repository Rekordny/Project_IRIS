CTC_temp_incoming_war_with_DOC = {
	CTC_debug_temp_war_flag = {
		visible = {
			TAG = CTC
			is_testing = yes
		}
		complete_effect = {
			set_country_flag = CTC_WAR_INCOMIMG
		}
	}
	CTC_temp_war_countdown = {
		activation = {
			has_country_flag = CTC_WAR_INCOMIMG
		}
		days_mission_timeout = 30
		war_with_on_timeout = DOC
		war_with_on_timeout = ELN
		war_with_on_timeout = URU
		timeout_effect = {
			#DOC = {
			#	diplomatic_relation = {
			#		country = URU
			#		relation = guarantee
			#	}
			#}
			CTC = {
				create_wargoal = {
					target = URU
					type = take_core_state
				}
				create_wargoal = {
					target = DOC
					type = take_core_state
				}
			}
			add_ai_strategy = {
				type = declare_war
				id = DOC
				value = 100
			}
			add_ai_strategy = {
				type = conquer
				id = DOC
				value = 100
			}
			add_ai_strategy = {
				type = declare_war
				id = DOC
				value = 100
			}
			add_ai_strategy = {
				type = declare_war
				id = DOC
				value = 100
			}
		}
	}
}
