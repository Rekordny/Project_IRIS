focus_tree = {
	id = WUT_focus
	
	country = {
		factor = 0
		
		modifier = {
			add = 10
			tag = WUT
		}
	}
	
	default = no

	continuous_focus_position = { x = 20 y = 1000 }

    #天瞠当绝贼 
    focus = {
		id = WUT_hero_become_thief
		icon = GFX_goal_generic_major_alliance
		x = 10
		y = 1
		cost = 1
		ai_will_do = {
			factor = 1
		}
		available = { WUT = { is_in_faction = yes } }
		
		completion_reward = {
			WUT = { country_event = { id = SRS.9 } }
		}
    }
}